

## Plan: Change Hero Background Color

Update the Hero component's background from deep teal (`#0B736D`) to true blue (`#001965`).

### File Modified
- `src/components/Hero.tsx` — change `backgroundColor: "#0B736D"` to `backgroundColor: "#001965"` (line ~68)

